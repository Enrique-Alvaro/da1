{
  "name": "crownbid-monorepo",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/mobile"
  ],
  "scripts": {
    "db:schema": "echo \"Run database/schema.sql in SQL Server\"",
    "dev:api": "npm --workspace apps/api run dev",
    "dev:mobile": "npm --workspace apps/mobile run dev"
  }
}
