{
  "name": "@crownbid/api",
  "private": true,
  "version": "0.0.1",
  "description": "CrownBid REST API (Express + TypeScript) — placeholder",
  "main": "dist/server.js",
  "scripts": {
    "dev": "echo \"[placeholder] Install deps then: npx ts-node-dev src/server.ts\"",
    "build": "echo \"[placeholder] Install deps then: tsc\"",
    "start": "echo \"[placeholder] Install deps then: node dist/server.js\"",
    "test": "echo \"[placeholder] Install deps then: run test runner\"",
    "db:test": "tsx scripts/test-db-connection.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "mssql": "^11.0.1"
  },
  "devDependencies": {
    "@types/mssql": "^9.1.5",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
