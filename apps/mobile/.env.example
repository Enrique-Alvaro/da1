# CrownBid Mobile — copy to .env (never commit .env)
# Used at build time (EAS) and by `expo start` for local development.

# Public API base URL (must include /api suffix)
EXPO_PUBLIC_API_URL=http://YOUR_SERVER_IP:3006/api
